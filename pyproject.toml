[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Function calling tool using constrained decoding with small LLMs"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "pydantic>=2.0",
    "numpy>=1.24",
]
